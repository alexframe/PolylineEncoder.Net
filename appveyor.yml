os: Visual Studio 2015
version: 1.0.{build}
install:
- dnvm upgrade -r clr
- dnu restore
build:
  publish_nuget: true
  publish_nuget_symbols: true
  include_nuget_references: true
  verbosity: minimal
